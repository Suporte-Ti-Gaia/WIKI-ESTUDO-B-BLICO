# Copie este arquivo para .env.local (uso local) e preencha com suas chaves.
# NUNCA envie suas chaves reais para o GitHub — este .env.example é só um modelo.
# Só é obrigatório preencher a chave do(s) provedor(es) que você pretende usar.

ANTHROPIC_API_KEY=sk-ant-sua-chave-aqui
GROQ_API_KEY=gsk_sua-chave-aqui
GEMINI_API_KEY=sua-chave-do-google-ai-studio-aqui
